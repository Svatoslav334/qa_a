 TESTCASES
###  URL:
https://qa-internship.avito.com

## Тестируемые ручки

| Метод | Endpoint | Описание |
|-------|----------|----------|
| POST | `/api/1/item` | Создать объявление |
| GET | `/api/1/item/{id}` | Получить объявление по ID |
| GET | `/api/1/{sellerID}/item` | Получить объявления по sellerID |
| GET | `/api/1/statistic/{id}` | Получить статистику объявления |

---

##  1. Создание объявления `POST /api/1/item`

###  Позитивные тесты

| ID | Название | Вход | Ожидаемый результат |
|----|----------|------|---------------------|
| TC-POST-01 | Успешное создание | Валидный JSON | `200 OK`, `status = success` |
| TC-POST-02 | Без статистики | Только sellerID, name, price | `200 OK`, дефолтные статистики `=0` |
| TC-POST-03 | Максимальные значения | Max int для цен и статистики | `200 OK` |

###  Негативные тесты

| ID | Название | Вход | Ожидаемый результат |
|----|----------|------|---------------------|
| TC-POST-04 | Отсутствует sellerID | Без поля `sellerID` | `400 Bad Request` |
| TC-POST-05 | Неправильный формат sellerID | Строка вместо числа | `400 Bad Request` |
| TC-POST-06 | Неправильный тип price | Строка вместо числа | `400 Bad Request` |
| TC-POST-07 | Пустое name | `name=""` | `400 Bad Request` |
| TC-POST-08 | Отрицательная цена | `price < 0` | `400 Bad Request` |


##  2. Получение объявления по ID `GET /api/1/item/{id}`

### Позитивные тесты

| ID | Название | Условие | Ожидаемый результат |
|----|----------|---------|---------------------|
| TC-GET-ID-01 | Получить существующее объявление | ID создан ранее | `200 OK`, JSON содержит все поля |
| TC-GET-ID-02 | Проверка формата | ID UUID | `200 OK`, поля корректных типов |

### Негативные тесты

| ID | Название | Условие | Ожидаемый результат |
|----|----------|---------|---------------------|
| TC-GET-ID-03 | Неверный формат ID | ID не UUID | `400 Bad Request` |
| TC-GET-ID-04 | Несуществующий ID | Рандомный UUID | `404 Not Found` |
| TC-GET-ID-05 | Пустой ID | `""` | `400 Bad Request` |


## 3. Получить объявления по sellerID `GET /api/1/{sellerID}/item`

### Позитивные тесты

| ID | Название | Вход | Ожидаемый результат |
|----|----------|------|---------------------|
| TC-GET-S-01 | Получить список | sellerID с объявлениями | `200 OK`, массив JSON |
| TC-GET-S-02 | У продавца нет объявлений | sellerID без объявлений | `200 OK`, пустой массив |

### Негативные тесты

| ID | Название | Вход | Ожидаемый результат |
|----|----------|------|---------------------|
| TC-GET-S-03 | Невалидный sellerID | строка | `400 Bad Request` |
| TC-GET-S-04 | sellerID < 111111 | слишком маленький | `400 Bad Request` |
| TC-GET-S-05 | sellerID > 999999 | слишком большой | `400 Bad Request` |

## 4. Получить статистику `GET /api/1/statistic/{id}`

### Позитивные тесты

| ID | Название | Условие | Ожидаемый результат |
|----|----------|---------|---------------------|
| TC-STAT-01 | Получить статистику | ID создан ранее | `200 OK`, JSON содержит `likes`, `viewCount`, `contacts` |

### Негативные тесты

| ID | Название | Условие | Ожидаемый результат |
|----|----------|---------|---------------------|
| TC-STAT-02 | Несуществующий ID | UUID без объявления | `404 Not Found` |
| TC-STAT-03 | Неверный формат | ID не UUID | `400 Bad Request` |

## Дополнительные проверки

- Проверка времени создания `createdAt` (ISO8601)
- Неизменность `id`
- Типы данных в статистике (int ≥ 0)
- Ответы должны содержать `Content-Type: application/json`

### 🚀 Всего тестов: **22**
